# headers file

# rules for all paths and files
/*
  # force https without preload
  Strict-Transport-Security: max-age=63072000; includeSubDomains

  # prevent coockies to be sended to other sites with js
  Set-Cookie: "HttpOnly;Secure;SameSite=Strict"

/*
  # set build_id in header
  Version: "$BUILD_ID"